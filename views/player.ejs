<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Details</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav>
        <a href="/">Home</a>
        <button id="backToTeam">Back to Team</button>
    </nav>
    <h1>Player Details</h1>
    <div>
        <img src="<%= player.profileImageUrl %>" alt="<%= player.name %>'s Profile Picture" style="width:200px;">
        <p><strong>Name:</strong> <%= player.name %></p>
        <p><strong>Age:</strong> <%= player.age %></p>
        <p><strong>Role:</strong> <%= player.role %></p>
        <p><strong>Team:</strong> <%= teamName %></p>
        <p><strong>Hobbies:</strong> <%= player.hobbies.join(', ') %></p>
    </div>
    <h2>Statistics</h2>
    <ul>
        <li><strong>Matches Played:</strong> <%= stats.matchesPlayed %></li>
        <li><strong>Rounds Won:</strong> <%= stats.roundsWon %></li>
        <li><strong>Headshots:</strong> <%= stats.headshots %></li>
        <li><strong>Kills:</strong> <%= stats.additionalStats.kills %></li>
        <li><strong>Deaths:</strong> <%= stats.additionalStats.deaths %></li>
        <li><strong>Rating:</strong> <%= stats.additionalStats.rating %></li>
    </ul>

    <!-- Gebruikt window.history.back om terug te gaan naar vorige pagina dat in dit geval het team is -->
    <script>
        const backToTeamButton = document.getElementById('backToTeam');

        if (backToTeamButton) {
            backToTeamButton.addEventListener('click', () => {
                window.history.back();
            });
        }
    </script>
</body>
</html>