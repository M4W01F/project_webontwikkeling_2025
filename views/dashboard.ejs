<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <title>Dashboard</title>
</head>
<body>
    <h2>Welkom, <%= user.username %></h2>
    <p>Je bent ingelogd als <strong><%= user.role %></strong></p>

    <% if (user.role === 'ADMIN') { %>
        <button onclick="window.location.href='/edit'">Admin Bewerken</button>
    <% } %>

    <button onclick="window.location.href='/logout'">Uitloggen</button>
</body>
</html>